mod suite { 
    #[test]
    fn number_of_timeslots() {
        debug_assert_eq(11, slot_names.count());
    }

    fn check_noon() {
        let test_data = tgm::load();
        debug_assert_eq(test_data.name = "midday")
    }
} // mod suite
// ---- end of file ----